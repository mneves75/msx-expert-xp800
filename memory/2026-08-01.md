# 2026-08-01 — Deployment Evidence

- The deployed response passed `verify-prod.mjs`: CSP contains no `'unsafe-inline'` in
  `script-src` or `style-src`, `object-src` and `frame-ancestors` are `'none'`, and HSTS,
  `nosniff`, and frame denial are present.
- Empty slots reached the built-in BASIC screen, CRT warmth reached **0.998**, and
  cartridge insertion promoted to real WebMSX under that CSP. The two blocked WebMSX
  `style-src` requests are expected and visually inert.
- `probe-game.mjs` passed **10/10** against the deployed artifact: splash, field,
  autonomous movement, wall death, restart, and 3D joystick input. The joystick proof
  produced `rowDiff 144`.
- Edge measurements were TTFB **144 ms**, document **354 ms**, and scene readiness
  **2.07 s**.
- The two open visual mismatches remain the console-top ratio (**0.85 rendered versus
  0.36 reference**) and keycap exposure (**rgb(172,169,162) versus rgb(184,181,175)**).
