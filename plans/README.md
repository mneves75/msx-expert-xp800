# Implementation Plans

Generated by the `improve` skill on 2026-08-07 at commit `b7369ef`. Execute in
the order below. The primary session owns integration and updates this index.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make responsive capture proof honest | P1 | M | — | DONE |
| 002 | Harden deployment and CI verification | P1 | M | 001 | DONE |
| 003 | Fix keyboard ownership across repeated physical events | P1 | S | 002 | DONE |
| 004 | Reset owned GPU state and complete procedural cache keys | P1 | M | — | DONE |
| 005 | Close deep-review lifecycle and probe gaps | P1 | M | 002–004 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED.

## Dependency notes

- 002 follows 001 because deployment screenshots must use the corrected responsive
  camera and capture vocabulary.
- 003 follows 002 because both edit `tools/verify-interactions2.mjs`; keeping them
  sequential avoids conflicting test instrumentation.
- 004 is source-independent and may be executed alongside 001–003.

## Findings considered and rejected

- Change the shared `panelBlack()` material to the keyboard-specific `#141414`:
  rejected. SPEC/MEMORY explicitly call this the **keyboard** inset calibration;
  `Keyboard.ts` applies it to a clone. Main-unit and CRT callers use the generic base.
- Upgrade every outdated dependency: rejected. The audit found no known vulnerability
  and no reachable defect fixed by an upgrade; unrelated churn is not a fix.
- Add named scripts for every diagnostic or reorganize `tools/probes/`: rejected as
  convenience-only work unrelated to correctness.
- Add machine-sensitive performance baselines to CI: rejected until stable counters
  and runner variance have a measured budget.
- Change graphite albedo or global exposure to close the open lighting mismatch:
  rejected pending confirmation that the calibration ROIs select the intended surfaces.

## Verification caveats

- The repository's pnpm wrapper could not open its external v11 store and then proposed
  removing `node_modules`; no destructive reinstall was accepted. The exact pinned local
  TypeScript, ast-grep, Vite, and Node syntax commands passed instead.
- The current staging deployment predates these review fixes. Its header, CSP, WebMSX,
  and cartridge checks pass, but the new `Engine.isHealthy` assertion correctly cannot
  pass until a future authorized staging deployment includes this source.
